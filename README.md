Commercelink Spring Boot Starter

A unified internal starter that auto-configures core infrastructure components for Commercelink microservices.

Instead of maintaining multiple small libraries, this project provides one starter module that wires everything automatically based on environment and properties.